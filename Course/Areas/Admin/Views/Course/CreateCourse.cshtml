@model CourseApp.Areas.Admin.Models.CreateCourseDTO
@addTagHelper*,Microsoft.AspNetCore.Mvc.TagHelpers
@{
    ViewData["Title"] = "CreateCourse";
    Layout = "~/Areas/Admin/Views/Shared/_adminLayout.cshtml";
}

<!-- Visible labels Form start -->
<div class="col-lg-12 top20">
    <div class="widgets-container">


        <h5 class="top30">Column Sizing</h5>
        <hr>
        <form method="post" enctype="multipart/form-data">
            <div class="row">
                <div class="col-md-4">
                    <input asp-for="CourseName" class="form-control" placeholder="course name" type="text">
                </div>
                <div class="col-md-4">
                    <input asp-for="Price" class="form-control" placeholder="price" type="text">
                </div>
            </div>
            <br />
            <div class="row">
                <div class="col-md-4">
                    <select asp-for="TeacherId" asp-items="@ViewBag.Teachers" class="form-control" ></select>
                </div>
                <div class="col-md-4">
                    <select asp-for="CourseCategoryId" asp-items="@ViewBag.CourseCategory" class="form-control"></select>
                </div>
            </div>
            <br />
            <div class="row">
                <div class="col-md-4">
                    <input asp-for="CourseImage" class="form-control"  type="file">
                </div>                
            </div>
            <br />
            <div class="row">
                <div class="col-md-12">
                    <textarea asp-for="CourseDescription" id="editor" class="form-control"> </textarea>
                  </div>
            </div>
            <br />
           <button class="btn btn-primary" type="submit">Save</button>
        </form>
    </div>
</div>
<!--Visible labels Form End -->
<script>
    ClassicEditor
        .create(document.querySelector('#editor'), {
        })
        .then(editor => {
            window.editor = editor;
        })
        .catch(err => {
            console.error(err.stack);
        });
</script>
<script src="~/ckeditor5-build-classic/ckeditor.js"></script>